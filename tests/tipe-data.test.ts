describe('Data Type', function () {
  it('should must declare', function () {
    let name: string = "Akira Raihan"
    let balance: number = 1000;
    let isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);

    // name = 100;
    // balance = "akira";
    // isVip = 100;
  });
});