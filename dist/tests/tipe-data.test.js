"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        let name = "Akira Raihan";
        let balance = 1000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 100;
        // balance = "akira";
        // isVip = 100;
    });
});
